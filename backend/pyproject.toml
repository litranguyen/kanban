[project]
name = "kanban-backend"
version = "0.1.0"
description = "FastAPI backend for the Kanban MVP"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.116.0",
  "uvicorn[standard]>=0.23.0",
]

[tool.uv]
# uv package manager configuration
